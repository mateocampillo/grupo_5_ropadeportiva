<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/header_footerMain.css">
    <link rel="stylesheet" href="/css/ProductList.css">
    <title>Listado de productos</title>
</head>
<body>
    <%- include("../partials/header.ejs") %> 
    <main>
        <h2>Listado general de productos</h2>
        <div class="container-general">
            <% for(let i = 0; i < productos.length; i++){ %> 
            <div class="container-productos">
                <a href="/productos/detalle/<%= productos[i].dataValues.id %>">
                    <div class="container-imagen"><img src="/images/ProductDetail/<%= productos[i].dataValues.img1 %>" alt="Imagen"></div>
                    <div><p>$ <%= productos[i].dataValues.price %></p></div>
                    <div><h3><%= productos[i].dataValues.name %></h3></div>
                </a>
            </div>
            <% } %> 
        </div>
    </main>
    <%- include("../partials/footer.ejs") %> 
</body>
</html>
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@300&family=Rubik:wght@300&display=swap" rel="stylesheet">
    <script src="https://kit.fontawesome.com/6e4f7b7266.js" crossorigin="anonymous"></script>
    <link rel="stylesheet" href="/css/header_footerMin.css">
    <link rel="stylesheet" href="/css/Login.css">
    <title>Todo deportes</title>
</head>
<body>
    <%- include("../partials/headerUsers") %>
    <main>
        <div class="contenedor">
            <h2>Entrar</h2>
            <hr>
            <h2 class="err-credenciales"><%= err %></h2>
            <form class="formulario-contenedor" action="/users/login" method="post">
                <div class="form-div-texto">
                    <label for="emailUserEmail">Email</label>
                    <input type="email" name="emailUserEmail" id="emailUserEmail" required minlength="6"  value="<%= locals.old && old.emailUserEmail %>">
                    <% if (locals.errors && errors)  { %>
                        <% errors.forEach(error => { %>
                            <% if (error.param == 'emailUserEmail') { %>
                                <p class="erroresValidation"><%= error.msg %></p>
                            <% } %> 
                        <% }) %>
                    <% } %>
                </div>
                <div class="form-div-texto">
                    <label for="txtPassword">Contraseña</label>
                    <input type="password" name="txtPassword" id="txtPassword" required minlength="6">
                    <% if (locals.errors && errors)  { %>
                        <% errors.forEach(error => { %>
                            <% if (error.param == 'txtPassword') { %>
                                <p class="erroresValidation"><%= error.msg %></p>
                            <% } %> 
                        <% }) %>
                    <% } %>
                </div>
                <!-- <div class="chkRemember">
                    <label for="chkRemember">Recordarme</label>
                    <input type="checkbox" name="chkRemember" id="chkRemember" checked>
                </div> -->
                <div class="contenedor-submit">
                    <button type="submit">Ingresar</button>
                </div> 
                <div class="recuperar-contraseña">
                    <a href="/users/recover">¿Olvidaste tu contraseña?</a>
                </div>
            </form>
            <hr>
            <h4>¿Aún no te registraste?</h4>
            <a href="/users/register">Registrarme</a>
        </div>
    </main>
    <%- include("../partials/footerUsers") %>
</body>
</html>